angular.modules('sendhalp', [

])
.config(function($routeProvider, $locationProvider) {
  $routeProvider
    .when('', {

    })
    .when('', {

    })
    .when('', {

    })
    .otherwise({

    });
  //templateUrl
  //controller
  //redirectTo

  // browse
  // saved
  // submit
  //404
  $locationProvider.hashPrefix('');
});