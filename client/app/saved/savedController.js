angular.module('sendhalp.saved', [])
.controller('SavedController', function($scope, Saved) {
  // show food on left, Saved.savedEntries["food"]
  // show drinks on right, Saved.savedEntries["drink"]
  // Saved.removeEntry(id, category)
});